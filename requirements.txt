numpy==2.5.2
scipy==1.18.1
pandas==3.0.5
flask==3.1.3
gunicorn==23.0.0
pytest==9.1.1

# Needed only in scripts/validate_against_sklearn.py (external validation
# reference) and scripts/prepare_sample_datasets.py (bundled example CSVs).
# Not used by src/pca_explorer/ to perform the decomposition itself.
scikit-learn==1.9.0
